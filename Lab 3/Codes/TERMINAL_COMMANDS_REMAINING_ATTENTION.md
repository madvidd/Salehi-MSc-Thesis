# Lab 3 remaining attention experiments

Use [`TERMINAL_COMMANDS_LAB3_RECOVERY.md`](TERMINAL_COMMANDS_LAB3_RECOVERY.md).

The sequence excludes `baseline_mha` and runs `qknorm`, `talking_heads`, and
`qknorm_talking_heads`. Each variant must be archived locally and its small
result package must be pulled, merged into `main`, pushed, and remotely verified
before the sequence advances. Native failures receive four checkpoint-aware
attempts; persistent failures are published as failure diagnostics rather than
mislabelled as completed results.